import pandas as pdfrom commodplot import commodplot as cpldef seas_chart(df:pd.DataFrame, series: str, title: str):    fig = cpl.seas_line_plot(df[series], title=title, shaded_range=5, hist_freq='W')    return fig